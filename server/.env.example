# ===== 华为配置生成器 — 环境变量 =====
# 复制此文件为 .env 并填入实际值

# 服务端口 (Render 会自动注入 PORT，本地默认 3001)
PORT=3001

# 允许的前端域名 (CORS)，多个用逗号分隔
# 部署到 Vercel 后填写实际 Vercel 域名
ALLOWED_ORIGINS=https://your-app.vercel.app

# Node 环境
NODE_ENV=production
